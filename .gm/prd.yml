- id: link-glitch-verify
  subject: 'HARDWARE VERIFY: measure that Link-over-WiFi does not glitch audio (xrun count with Link active over WiFi/AP, vs looper once-a-second glitch). Test: run aloop with Link over WiFi, monitor ALSA xruns + the telemetry glitch counter over 10min.'
  notes: Needs real Pi4. Buildable parts (Link lib integration, IRQ affinity config) done separately.
  blockedBy:
  - external
  - no-Pi4-in-session — documented on-hardware xrun test
  status: pending
- id: wifi-ap-multicast
  subject: 'HARDWARE VERIFY: a peer device Links over the Pi-hosted AP (multicast passes with ap_isolate=0). Test: connect a phone/laptop to the aloop AP, open a Link-enabled app, confirm tempo sync. Config done; behavior needs hardware+a peer.'
  notes: ap_isolate=0 set; witnessing needs Pi4 + a Link peer.
  blockedBy:
  - external
  - no-Pi4-in-session — peer Link over AP test
  status: pending
- id: wifi-ap-irq-affinity
  subject: 'HARDWARE VERIFY: confirm WiFi/USB IRQs steer off the audio cores on Pi4 and the audio cores stay clean under AP load. Test: cat /proc/irq/*/smp_affinity after rt-tune.sh, run cyclictest on cores 1,3 under hostapd load. Config in rt-tune.sh; the MEASUREMENT needs hardware.'
  notes: rt-tune.sh implements it; witnessing needs Pi4.
  blockedBy:
  - external
  - no-Pi4-in-session — smp_affinity + cyclictest under AP load
  status: pending
- id: usb-gadget-alsa-bridge
  subject: 'Bridge the f_uac2 gadget ALSA endpoint to the DSP audio callback: the gadget presents an ALSA PCM the audio thread reads/writes. Wire capture (host->Pi) and playback (Pi->host) at the exact block/rate.'
  notes: How gadget audio reaches loopMachine. ALSA PCM mmap for low latency.
  status: pending
- id: usb-gadget-latency-verify
  subject: 'HARDWARE VERIFY: f_uac2 round-trip latency parity vs 1.333ms baseline; kernel microframe handling correct (no -4608-class corruption). Test: loopback measurement Pi-as-device to a host, compare to looper. Needs hardware.'
  notes: Latency + correctness measurement.
  blockedBy:
  - external
  - no-Pi4-in-session — f_uac2 round-trip latency measurement
  status: pending
- id: rt-cyclictest-verify
  subject: 'HARDWARE VERIFY: cyclictest worst-case wakeup < block budget under audio+network load, xrun-free over a long run. Test: cyclictest -m -S -p95 -i200 -h400 while aloop runs Link-over-WiFi. Documented in RT-TUNING.md; needs hardware.'
  notes: The RT proof. cyclictest command documented.
  blockedBy:
  - external
  - no-Pi4-in-session — cyclictest under load
  status: pending
- id: control-plane-port
  subject: 'Port the control plane (looper Core2): MIDI (ALSA rawmidi replacing Circle USB-MIDI), telemetry UDP sockets (:4444/:4445/:4446 -> BSD sockets), the apcKey25 param logic, Link tick. Runs on the control core.'
  notes: Core2 control plane on Linux. APC MIDI + telemetry + Link.
  status: pending
- id: midi-apc-port
  subject: 'Port APC Key25 USB-MIDI: Circle USB-MIDI -> Linux ALSA rawmidi/snd-usb-audio. The CC/note handling (apcKey25*.cpp) is portable logic; only the input source changes. Verify knobs/notes reach the param snapshot.'
  notes: MIDI control input. Mature Linux driver; the mapping logic ports unchanged.
  status: pending
- id: ci-green-witness
  subject: 'Ensure the CI pipeline goes green and is witnessed (CONSOLIDATE requirement): the workflows must actually pass on push. Fix any red before COMPLETE.'
  notes: CI must be green — the gm CONSOLIDATE phase witnesses this.
  status: pending
- id: x-hardware-dependency
  subject: 'CORNER: many verify rows (RT jitter, f_uac2 latency, Link-no-glitch, AP-multicast) need REAL Pi4 hardware. Session has no Pi. These stay blockedBy:[external, no-Pi4-in-session] with the exact on-hardware test documented, NOT falsely resolved. The BUILD (code/config/CI/docs) is fully reachable now; only on-device MEASUREMENT is external.'
  notes: 'Honesty: separate what is buildable-now (all code/config/docs/CI) from what needs hardware (measurements). No false completion.'
  status: pending
- id: x-consolidate-commit
  subject: 'CONSOLIDATE: commit everything as lanmower (no Claude attribution), push to AnEntrypoint/aloop main, witness CI green. The terminal step of this planning+scaffold session.'
  notes: The commit/push/CI-green close per gm CONSOLIDATE.
  status: pending
- id: dsp-port-verify-hw
  subject: 'HARDWARE VERIFY (partial): full end-to-end audio through the device (USB in -> DSP -> LV2 -> USB out) on real hardware. The DSP A/B correctness is verifiable NOW via the dubfx harness (no hardware); only the live device round-trip needs a Pi4.'
  notes: DSP correctness = dubfx harness (now). Live device = hardware.
  blockedBy:
  - external
  - no-Pi4-in-session — live device round-trip
  status: pending
