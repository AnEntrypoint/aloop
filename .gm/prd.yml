- id: x-hardware-dependency
  subject: 'CORNER: many verify rows (RT jitter, f_uac2 latency, Link-no-glitch, AP-multicast) need REAL Pi4 hardware. Session has no Pi. These stay blockedBy:[external, no-Pi4-in-session] with the exact on-hardware test documented, NOT falsely resolved. The BUILD (code/config/CI/docs) is fully reachable now; only on-device MEASUREMENT is external.'
  notes: 'Honesty: separate what is buildable-now (all code/config/docs/CI) from what needs hardware (measurements). No false completion.'
  status: pending
- id: hardware-test-execution
  subject: EXECUTE all test/hardware/*.sh on a real Raspberry Pi 4 and record the numbers (RT jitter, f_uac2 latency, Link no-glitch, AP multicast). This is the ONLY genuinely hardware-blocked item — the harnesses are all built + one-command runnable.
  notes: The single honest blocked-on-hardware row.
  blockedBy:
  - external
  - no-Pi4-in-session — run the completed test/hardware/ suite on a Pi4
  status: pending
- id: wire-user-lv2-runblock
  subject: 'GAP found in confirming pass: audio_thread.cpp runs the Faust home stack but the USER LV2 on Core3 is only a COMMENTED call site (// host.runBlock). Actually instantiate the Lv2Host in the audio thread, load /effects/user, and call host.runBlock() per block after the home stack — so the user swappable effect genuinely runs on the free core. Goal #2 requires this to be live, not commented.'
  notes: 'Real reachable gap: the user-FX path is designed+built but not invoked from the audio loop. Wire it + rebuild green.'
  status: pending
- id: wire-midi-thread
  subject: 'GAP: main.cpp starts Link/telemetry/audio but never spawns the MIDI loop (runMidiLoop) — so the control surface (the APC knobs/commands) is not running. Spawn runMidiLoop on the control thread + a shared ParamStore, and wire the param values into the Faust home stack + the Link/loop params. Goal completeness: the device must respond to its controller.'
  notes: 'Real reachable gap: MIDI input built (midi.cpp) but not spawned. Wire it in main.'
  status: pending
