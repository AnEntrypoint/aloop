---
key: mem-0c9ef758274d5c07-1002
ns: default
created: 1783935869404
updated: 1783935869404
---

## Alpine RPi diskless netboot init mechanism — WITNESSED from real initramfs-rpi (3.20.3)

Extracted boot/initramfs-rpi (gzip cpio, 289 files) and read /init (992 lines). CORRECTS prior wrong diagnosis (nlplug-before-configure_ip ordering was NOT the failure). do_networking=true iff ip= set OR http apkovl/alpine_repo (L521). No root= on cmdline (stock='modules=loop,squashfs,sd-mod,usb-storage quiet console=tty1') so the block-device path L638 is skipped. L710 do_networking sets repoopts=-n (nlplug -n does not require local boot repo), L718 nlplug-findfs -n, L725 configure_ip -> udhcpc -i eth0 SENDS initramfs DHCP. L872 find_boot_repositories echoes http alpine_repo -> apk fetches over net. Served tree correct: $OUT/apks/aarch64/APKINDEX + boot/modloop-rpi + aloop.apkovl.tar.gz present. Mechanism+tree are RIGHT; NFS-root rearchitecture is unnecessary. Live 'only firmware DHCP -> emergency shell' needs a serial-console capture of initramfs udhcpc/eth0 to see why DISCOVER never went out.
