# aloop STA-mode config. Add your known networks here; autoAP joins one of these
# when available and falls back to hosting the AP (above) when none are.
ctrl_interface=/var/run/wpa_supplicant
update_config=1
country=US

# network={
#     ssid="YourHomeWiFi"
#     psk="yourpassword"
# }
